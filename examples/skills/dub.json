{
	"name": "skills",
	"description": "MCP Skills example (server + self-verifying client e2e) for mcp.d — SEP-2640 Agent Skills over resources",
	"license": "Apache-2.0",
	"dependencies": {
		"mcp-d": { "path": "../.." },
		"examples-common": { "path": "../common" }
	},
	"configurations": [
		{
			"name": "server",
			"targetType": "executable",
			"targetName": "skills-server",
			"mainSourceFile": "server.d"
		},
		{
			"name": "client",
			"targetType": "executable",
			"targetName": "skills-client",
			"mainSourceFile": "client.d"
		}
	]
}
