{
	"name": "streaming",
	"description": "MCP example: progress, logging, cancellation + typed elicitation/sampling over stdio AND Streamable HTTP (server + self-verifying client e2e). Issue #357 / #436 / #437.",
	"license": "Apache-2.0",
	"dependencies": {
		"mcp-d": { "path": "../.." },
		"examples-common": { "path": "../common" }
	},
	"configurations": [
		{
			"name": "server",
			"targetType": "executable",
			"targetName": "streaming-server",
			"mainSourceFile": "server.d"
		},
		{
			"name": "client",
			"targetType": "executable",
			"targetName": "streaming-client",
			"mainSourceFile": "client.d"
		}
	]
}
