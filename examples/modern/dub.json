{
	"name": "modern",
	"description": "The modern (2026-07-28) MCP protocol over stdio and Streamable HTTP: server/discover, per-request _meta, enableModern()/connect(), CacheableResult freshness hints",
	"license": "Apache-2.0",
	"dependencies": {
		"mcp-d": { "path": "../.." },
		"examples-common": { "path": "../common" }
	},
	"configurations": [
		{
			"name": "server",
			"targetType": "executable",
			"targetName": "modern-server",
			"mainSourceFile": "server.d"
		},
		{
			"name": "client",
			"targetType": "executable",
			"targetName": "modern-client",
			"mainSourceFile": "client.d"
		}
	]
}
